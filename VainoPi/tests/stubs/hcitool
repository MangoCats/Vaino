#!/bin/sh
printf '%s\n' "hcitool $*" >> "$VT_STATE/calls"
case "$1" in
    con) cat "$VT_STATE/hci_con" 2>/dev/null ;;
    afh) printf 'AFH map: 0x%s\n' "$(cat "$VT_STATE/afh" 2>/dev/null)" ;;
esac
exit 0
