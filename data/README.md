# `data/`

`vaino_new.db` is the current, live, working library database — every tool and doc example in this repo means this file when it says "the library." Everything else here (`.bak`, `.pre-*`-suffixed, or dated) is a historical snapshot, never live.
